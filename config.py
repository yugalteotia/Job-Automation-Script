# config.py
CSV_FILE_PATH = 'todaysProperties.csv'
API_URL = 'http://127.0.0.1:5000/api'   # replace the url with the actual url.
BEARER_TOKEN = 'test_mock_token'        # replace token with the actual and valid token.